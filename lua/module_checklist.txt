This is written in hierarchy.
module name (description) [progress / owner]

Architecture rule:
    luasquare_module = reusable Garry's Mod map-simulation infrastructure.
    luasquare_powerplant = reusable power/thermal/fluid plant systems.
    luasquare_dfr = Dark Fusion Reactor simulation and gameplay systems.
    bootstrapper scripts = map-repository owned; they know VMF targetnames.


luasquare_module
    +existing modules.
        map/ (manifest/component compiler, trusted package catalog, transactional lifecycle,
            declarative adapters, graph/cell editor and bounded subscribed inspection) [implemented; experiment_rbmk acceptance passed]
            - Canonical luasquare.map/v1 and luasquare.components/v1 sources; drafts never auto-load.
            - Owns source activation and rollback; physical cycles differ from construction dependencies.
            - experiment_rbmk is migrated; DFR is deferred and parent migration remains Planned.
            - Canonical source-text inspection preserves empty containers across realms; the graph editor has contextual
              typed linking, live snapped placement, border-routed links with one draggable anchor, movable generated
              positions, filters, hover inspection and a splitter. Its source manager uses explicit packed/draft loading,
              a draft file browser and overwrite confirmation; manifest/startup settings remain in the main toolbar.
        3d2display/ (source compiler, server runtime, chunked networking, client renderers,
            raycast interaction, graph history, shared themes, and admin editor) [implemented]
            - v1 JSON supports Simple lines and Complex pages/layered elements.
            - Packed sources live in map-owned data_static; editor drafts live in data/.
            - Runtime values/actions are named server registrations, never source Lua.
        gaugedisplay.lua (Source entity gauge display driver) [existing]
        control/ (JSON compiler, authoritative physical/virtual controls, owned keypads/targets,
            requests/owner locks/faults/history, display/timeline adapters and admin editor) [implemented; experiment_rbmk acceptance passed]
            - Map-owned luasquare.control/v1 sources replace controlbinding.lua and keypad_controller.lua.
            - Actual/accepted positions are separate; combined presses lock on the next tick without bypass.
            - Expected action callback rejection is terminal without a sticky fault; runtime/input failures still lock.
        seg7display.lua (skin-based pseudo 7-segment displays; explicit bindings and bounded SEG7 prefix discovery) [existing; declarative adapter implemented]
        annunciator/ (v1 JSON compiler, authoritative lifecycle/audio runtime, bounded networking,
            3D2D/timeline actions, client state, and admin editor) [implemented]
            - Packed multi-group sources live in map-owned data_static; drafts live in data/.
            - Conditions consume named telemetry providers and never source Lua.
            - The experiment_rbmk reference owns one provider and a packed 22-alarm source.
        RBMK selection is owned by luasquare_rbmk/selector.lua per core; the shared singleton selector is retired.

    logging.lua (shared logging, log history, module prefix, severity, developer print policy) [needed]
        - Should replace ad-hoc DFR.Log style later.
        - Should support loud critical errors and softer presentation warnings.

    runtime.lua (module lifecycle, central safe tick helpers, halt/error policy) [needed]
        - Owns safe pcall wrappers, critical halt behavior, timer start/stop helpers.
        - DFR, RBMK, and future map sims should not each reinvent this.

    statemachine.lua (generic named states and validated transitions) [needed]
        - DFR statecontroller should use this.
        - Useful for future startup timelines, disaster stages, and other maps.

    signalbus.lua (light event/value dispatch between modules) [needed later]
        - Named events, value updates, and optional metadata without becoming a visual node editor.
        - Useful for alarms, trips, display updates, DECAOS triggers, and debug history.
        - Typed ports can remain deferred, but the event vocabulary should become less ad-hoc.

    variables.lua (registered value definitions with units, limits, and display metadata) [needed later]
        - Documents type, unit, min/max, normal range, network/display precision, and failure behavior.
        - Helps DFR avoid hidden magic variables and helps displays show unavailable/stale data clearly.

    mapbinding.lua / sourcebinding.lua (logical map binding registry) [prototype implemented in luasquare_module/sourcebinding.lua]
        - Stores logical id, targetname, expected class, required level, notes.
        - Validates missing map entities and reports clear developer errors.
        - Later should support listening to Source outputs as well as firing Source inputs.
        - Current prototype supports first/all entity resolution for shared targetnames and Source input firing.

    control/ (shared JSON-driven Control Layer) [implemented; replaces removed prototype]
        - Trusted registrations own behavior; map JSON owns control instances and keypads.
        - One keypad member declares mandatory digit/submit keys and optional clear/backspace; compiler owns physical subcomponents.
        - Server validates physical outputs, actor context, owner locks and asynchronous requests.
        - RBMK/DFR operators use this layer; local previews are isolated from packed gameplay.

    machinery.lua (Hammer movable entity controller) [prototype implemented in luasquare_module/machinery.lua]
        - Controls func_tracktrain, func_rotating, func_door_rotating, func_movelinear, doors, relays.
        - Provides deploy/retract/open/close/start/stop/set speed/set position helpers.
        - Should not contain DFR-specific shaft or stabilizer names.
        - Current prototype adds path registration, destination-aware tracktrain movement, and path_track pass callbacks.

    timeline/ (v1 source compiler, authoritative choreography, editor and bounded networking) [implemented]
        - Absolute timed events, scoped owners, named channels, parent/child runs,
          failure state, cancellation, pause/resume, owner locks, guards, and state handoff.
        - Wait conditions and branches remain future extensions.
        - Should coordinate presentation with simulation but not replace simulation.
        - Needed later for automatic startup, DECAOS sequences, disasters, music sections.

    audio/ (v1 source-driven music, PA, soundscapes, buses, subtitle sequences and editors) [implemented]
        - Plays map-wide sounds, localized sounds at targetnames, music sections, and soundscape toggles.
        - Should avoid requiring huge ambient_generic chains.
        - Should expose current music/sound state for late join sync where practical.

    audio/ subtitle transport (networked subtitle/display text channel) [implemented]
        - Generic speaker label, text, duration, priority, optional sound id.
        - DECAOS uses this, but the subtitle transport should be reusable.

    netstate.lua (small replicated state snapshots for late join and UI) [needed]
        - 3D2D already broadcasts display state, but DFR also needs subtitles, music state, timeline phase, etc.
        - Should support rate-limited server-owned state.

    cleanup.lua (cleanup/reset hooks for framework declarations, registries, commands, and timers) [prototype implemented]
        - PreCleanupMap cancels timeline registries, removes named Luasquare/RBMK/DFR timers and commands, then clears framework globals.
        - A run-on-spawn lua_run can then include the map bootstrap against a fresh runtime.
        - Map bootstraps can register additional globals and timer prefixes when needed.

    sourcefx.lua (generic Source presentation/effect helpers) [needed later]
        - Relays, sprites, lights, skins, bodygroups, particles/effects if practical.
        - Possible future place for prop_coreball helpers.

    profiler.lua (developer performance counters for ticks, networking, and expensive callbacks) [deferred]
        - Requirements do not need a full UI yet, but instrumentation will help once graphs, timelines, and disasters stack up.


luasquare_powerplant
    +existing modules.
        endpoints.lua / catalog.lua / components.lua (typed reusable fluid/thermal capabilities and
            declarative plant adapters with explicit/generated electrical dependencies) [implemented; experiment_rbmk acceptance passed]
        components.lua telemetry snapshots (instance-qualified component data consumed directly by 3D2D) [implemented]
            - The reference no longer constructs generic telemetry.panel presentation placeholders.
        fluidnetwork.lua [existing]
        fluidvalve.lua [existing]
        fluidpump.lua [existing]
        heatexchanger.lua [existing]
        condenser.lua [existing]
        deaerator.lua [existing]
        steamseparator.lua [existing]
        turbine.lua [existing]
        powergenerator.lua [existing]
            - Sync commands are consumed by the generator: tripped generators ignore them, synchronized generators open
              their breaker, and unsynchronized generators attempt closure while retaining configured mismatch trips.
        powergrid.lua [existing]
        dieselgenerator.lua [existing]
        coolingtower.lua [existing]
        debug.lua [existing]

    Notes:
        - DFR may wrap these through facility/thermalplant.lua rather than exposing all plant machinery directly.
        - No immediate architecture change required unless DFR needs a generic feature.


luasquare_dfr
    init.lua (loads DFR package modules, not map-specific targetnames) [prototype exists]
    config.lua (DFR constants and development-tunable values) [prototype exists]

    runtime/state.lua (DFR top-level state progression) [prototype exists]
        - OFFLINE, STARTUP_PREP, MANUAL_STARTUP, ANNIHILATION_STAGE, DARK_FUSION_STAGE, shutdown, disaster, ending.
        - Should use generic luasquare_module/statemachine.lua once available.
        - Current runtime prototype should move toward a generic state machine.

    procedure/ (manual startup and pre-annihilation coordinators) [prototype exists]
        - Operator workflow logic belongs here, not in reactor physics.
        - Automatic startup should eventually perform the same steps manual startup uses.
        - Component choreography is invoked through owned child timelines.

    presentation/telemetry.lua (operator-facing data builders for 3D2D displays) [prototype exists]
        - Keep separate from debug.
        - Should read subsystem state and produce display lines only.

    runtime/debug.lua (developer/admin inspection and forced test commands) [prototype exists / expanded]
        - Snapshots, force state, force values, validation reports, test helpers.
        - Not the same thing as operator telemetry.
        - Current prototype adds admin/server console commands used by the DFR spawnmenu debug panel.

    autorun/client/luasquare_dfr_spawnmenu.lua (spawnmenu-accessed DFR debug and test controls) [prototype exists]
        - Avoids recompiling the VMF just to place temporary debug buttons.
        - Calls server-side debug concommands with admin/single-player checks.

    decaos.lua (DFR announcer/assistant brain) [needed]
        - Chooses DECAOS lines, warnings, authorization flavor, event narration.
        - Uses generic audiosystem.lua and subtitle.lua.

    alarm.lua (DFR logical alarms, trips, warnings, and notification priority) [needed]
        - Owns alarm active/latched/acknowledged state.
        - Feeds annunciators, telemetry, DECAOS, and timeline interruption.
        - Separate from the reusable annunciator display renderer.

    faults.lua (recoverable malfunctions, trips, degraded modes, and fault propagation) [needed later]
        - Should describe what failed, why it failed, what modules are affected, and how/if it can recover.
        - Disasters are terminal/high-level outcomes; faults are normal operating problems.

    quota.lua (power quota requests and fulfillment tracking) [needed later]
    powerexport.lua (credited export, internal consumption, export limits, and market value feed into quota/scoring) [needed later]
    scoring.lua (operator score, penalties, exported energy accounting) [needed later]
    emergency.lua (Antimatter Termination and emergency operator actions) [needed later]
    disaster.lua (Runaway Fusion stages, purge trigger, singularity breach trigger) [needed later]
    ending.lua (terminal ending state and post-ending behavior) [needed later]
    hazard.lua (player/NPC/environment hazard zones and damage policy) [needed later]
        - Core radiation, director beam, combine balls, high temperature, explosions, disaster effects.
        - Should connect to alarms and presentation, not silently punish players.
    devreset.lua (developer-only soft reset/test reset support) [deferred]
        - Normal public reset remains map reload.
        - Useful for long iteration once more subsystems exist.

    superstructure
        superstructure.lua (waste heat, global integrity, subsystem effectiveness, damage) [needed]
        catalyzer.lua (six catalyzers, bindings, modes, and owned operation timelines) [prototype exists]

    reactor
        core.lua (shared core state, containment sphere, event horizon, radiation, output aggregation) [needed]
        corevisual.lua (core props, beam groups, scalable visual basis, chamber light control) [prototype exists]
        reactormachine.lua (DFR shaft/stabilizer/director lens machinery wrapper for gm_darkfusion_v2 armature) [prototype exists]
        annihilation.lua (matter/antimatter injection, equilibrium, fuel saturation, annihilation power GW) [needed next]
        darkfusion.lua (influx/outflux, stored flux GWh, energy saturation, final export TW) [needed later]
        energysaturation.lua (thresholds, hysteresis, warning/damage/runaway events) [needed later]
        stabilizer.lua (containment field strength/stability, power draw, core inflation) [prototype exists]
        directorbeam.lua (beam active state, lens offsets, precision/imprecision) [prototype exists]

    facility
        facilitypower.lua (facility/internal power routing and consumption) [needed later]
        cooling.lua (DFR-level coolant availability and remote response delays) [needed later]
        thermalplant.lua (DFR wrapper around luasquare_powerplant heat/power systems) [needed later]
        resourcestorage.lua (matter, antimatter, fuel cells, stasis cells, storage hazards) [needed]
        darkfusionparticleenricher.lua (annihilation fuel and dark-fusion fuel production mechanics) [needed later]
        remoteplant.lua (slow Citadel-scale remote machinery response model) [needed later]
            - Lets coolant, enrichment, and secondary plant delays feel intentionally huge rather than laggy.

    entity
        fuelcellreceptor.lua (attached to enricher and catalyzer) [needed later]
        resourcecell.lua (actual spawnable scripted entity; discuss later) [deferred]
        stasiscell.lua (emergency purge cell entity/logic; may be a resourcecell type but should be explicit) [deferred]


Map repository bootstrapper scripts
    DarkEnergyConstruct-Reactor-Map/lua/luasquare_dfr/bootstrapper/gm_darkfusion_v2.lua [map-owned]
        - VMF targetnames, display anchors, machinery bindings, sound names, relays, map-specific rigging.
        - Should not live in Luasquare framework long-term.

    Luasquare-RBMK-Map-Framework/lua/luasquare_rbmk/bootstrapper/experiment_rbmk.lua [framework example map-owned here]
        - Cleanup-aware manifest loader; configuration/wiring live in data_static/luasquare/{map,components}/experiment_rbmk.
        - Independent cores, layouts/fuel/settings/selector/effects adapters belong in luasquare_rbmk.
        - Optional two-core and heat-exchanger fixtures require explicit manifest selection and map reload.


Migration notes
    - Move generic DFR prototypes into luasquare_module:
        runtime/binding.lua -> luasquare_module/sourcebinding.lua
        runtime/control.lua -> luasquare_module/control/ (complete; DFR retains trusted reactor actions/predicates)
        runtime/machinery.lua -> luasquare_module/machinery.lua
        DFR.Log parts -> luasquare_module/logging.lua
        DFR.Halt/tick safety parts -> luasquare_module/runtime.lua

    - Then shrink luasquare_dfr to actual reactor/facility/procedure code.
    - Avoid putting VMF targetnames inside reusable DFR modules.
    - Keep telemetry separate from debug.
    - Keep DECAOS-specific behavior in DFR, but subtitles/audio transport generic.


No need to touch luasquare_rbmk yet unless there has been a change in shared luasquare_module APIs.
